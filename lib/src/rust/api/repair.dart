// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'streaming.dart';

// These functions are ignored because they are not marked as `pub`: `cache_for`, `fetch_segment`, `file`, `locate`, `locator_for`, `new`, `run_repair_inner`, `run_repair_session`
// These types are ignored because they are neither used by any `pub` functions nor (for structs and enums) marked `#[frb(unignore)]`: `ActiveRepair`, `NntpRepairFetcher`
// These functions are ignored (category: IgnoreBecauseExplicitAttribute): `has_file`, `read_par2`, `read_range`

/// Onarım oturumunu başlatır ve kimliği hemen döndürür. Sonuç
/// [`await_repair`] ile alınır; ilerleme [`repair_progress`] ile yoklanır.
Future<BigInt> beginRepair({
  required ProviderConfigDto config,
  required String nzbPath,
}) => RustLib.instance.api.crateApiRepairBeginRepair(
  config: config,
  nzbPath: nzbPath,
);

/// Oturumun son ilerleme anlığını döndürür; oturum yoksa None.
Future<RepairProgressDto?> repairProgress({required BigInt sessionId}) =>
    RustLib.instance.api.crateApiRepairRepairProgress(sessionId: sessionId);

/// Oturum sonucunu bekler. Oturum iptal edilmişse açık hata döner.
Future<RepairReportDto> awaitRepair({required BigInt sessionId}) =>
    RustLib.instance.api.crateApiRepairAwaitRepair(sessionId: sessionId);

/// Oturumu iptal eder. Kimlik artık aktif değilse false döner.
Future<bool> cancelRepair({required BigInt sessionId}) =>
    RustLib.instance.api.crateApiRepairCancelRepair(sessionId: sessionId);

/// Onarım oturumunun ilerleme anlığı.
class RepairProgressDto {
  /// "loading" | "verifying" | "solving" | "repairing" | "writing" | "done"
  final String phase;
  final BigInt completedBytes;
  final BigInt totalBytes;
  final String detail;

  const RepairProgressDto({
    required this.phase,
    required this.completedBytes,
    required this.totalBytes,
    required this.detail,
  });

  @override
  int get hashCode =>
      phase.hashCode ^
      completedBytes.hashCode ^
      totalBytes.hashCode ^
      detail.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is RepairProgressDto &&
          runtimeType == other.runtimeType &&
          phase == other.phase &&
          completedBytes == other.completedBytes &&
          totalBytes == other.totalBytes &&
          detail == other.detail;
}

/// Onarım sonucu. `clean` true ise set hasarsız çıktı ve katman gerekmedi.
class RepairReportDto {
  final int verifiedFiles;
  final int damagedSlices;
  final int repairedSlices;
  final bool clean;

  /// Katman diske yazıldıysa true; sonraki oynatma onarılan bölgeleri
  /// yerelden servis eder.
  final bool overlayActive;

  const RepairReportDto({
    required this.verifiedFiles,
    required this.damagedSlices,
    required this.repairedSlices,
    required this.clean,
    required this.overlayActive,
  });

  @override
  int get hashCode =>
      verifiedFiles.hashCode ^
      damagedSlices.hashCode ^
      repairedSlices.hashCode ^
      clean.hashCode ^
      overlayActive.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is RepairReportDto &&
          runtimeType == other.runtimeType &&
          verifiedFiles == other.verifiedFiles &&
          damagedSlices == other.damagedSlices &&
          repairedSlices == other.repairedSlices &&
          clean == other.clean &&
          overlayActive == other.overlayActive;
}
